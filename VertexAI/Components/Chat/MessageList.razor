@* 消息列表组件 *@
@inject MarkdownService Markdown

<main class="flex-1 overflow-y-auto px-3 py-4 bg-gradient-to-b from-sky-50/50 to-slate-50" id="messages-container" @onclick="OnAreaClicked">
    <div class="max-w-4xl mx-auto space-y-4">
        @if (Messages.Count == 0)
        {
            @* 精致的空状态 *@
            <div class="flex flex-col items-center justify-center py-20 text-center">
                <div class="w-20 h-20 mb-6 rounded-full bg-gradient-to-br from-sky-400 to-emerald-400 flex items-center justify-center shadow-xl">
                    <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"/>
                    </svg>
                </div>
                <h2 class="text-xl font-bold text-slate-700 mb-2">@PresetName</h2>
                <p class="text-slate-500 mb-6">开始一段新对话吧</p>
                <div class="flex flex-wrap gap-2 justify-center max-w-md">
                    <span class="px-3 py-1.5 bg-white/80 border border-slate-200 rounded-full text-xs text-slate-600 shadow-sm">
                        💡 试试问我任何问题
                    </span>
                    <span class="px-3 py-1.5 bg-white/80 border border-slate-200 rounded-full text-xs text-slate-600 shadow-sm">
                        🧠 支持深度思考模式
                    </span>
                    <span class="px-3 py-1.5 bg-white/80 border border-slate-200 rounded-full text-xs text-slate-600 shadow-sm">
                        📝 上下文记忆对话
                    </span>
                </div>
            </div>
        }

        @foreach (var msg in Messages)
        {
            <MessageBubble
                IsUser="@msg.IsUser"
                Content="@msg.Content"
                ThinkingContent="@msg.ThinkingContent"
                IsStreaming="@msg.IsStreaming"
                MarkdownHtml="@(msg.IsUser ? "" : Markdown.ToHtml(msg.Content))" />
        }

        @if (IsLoading && !Messages.Any(m => m.IsStreaming))
        {
            @* 骨架屏加载态 *@
            <div class="flex justify-start">
                <div class="bg-white border border-slate-100 rounded-2xl rounded-bl-sm px-4 py-3 shadow-sm max-w-[70%]">
                    <div class="flex items-center gap-2 mb-2">
                        <div class="w-2 h-2 rounded-full bg-sky-400 animate-pulse"></div>
                        <span class="text-slate-600 text-sm font-medium">正在思考...</span>
                    </div>
                    <div class="space-y-2">
                        <div class="h-3 bg-slate-100 rounded w-full animate-pulse"></div>
                        <div class="h-3 bg-slate-100 rounded w-4/5 animate-pulse" style="animation-delay: 0.1s"></div>
                        <div class="h-3 bg-slate-100 rounded w-3/5 animate-pulse" style="animation-delay: 0.2s"></div>
                    </div>
                </div>
            </div>
        }
    </div>
</main>

@code {
    [Parameter] public List<ChatMessageModel> Messages { get; set; } = [];
    [Parameter] public string PresetName { get; set; } = "默认助手";
    [Parameter] public bool IsLoading { get; set; }
    [Parameter] public EventCallback OnAreaClicked { get; set; }
}
