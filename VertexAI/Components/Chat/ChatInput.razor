@* 聊天输入区域组件 *@
@inject IJSRuntime JS
@implements IDisposable

<footer class="flex-shrink-0 bg-white/70 backdrop-blur-xl border-t border-slate-100 px-2 sm:px-3 py-3 sm:py-4 safe-area-bottom">
    <div class="max-w-4xl mx-auto">
        @* 图片预览区域 *@
        <ImagePreview Images="PendingImages" OnRemove="HandleRemoveImage" />

        <div class="flex gap-1.5 sm:gap-2 items-center">
            @* 上传按钮 - 使用 label + for 实现 iOS 兼容 *@
            <InputFile id="chat-file-upload"
                       OnChange="HandleFileSelected"
                       Accept="image/jpeg,image/png,image/webp,image/gif"
                       multiple
                       class="hidden" />
            <label for="chat-file-upload"
                   class="h-10 w-10 sm:h-12 sm:w-12 bg-slate-100 hover:bg-slate-200 text-slate-600 rounded-xl sm:rounded-2xl cursor-pointer transition-all flex items-center justify-center flex-shrink-0 @(IsLoading ? "opacity-50 pointer-events-none" : "")">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                </svg>
            </label>

            <div class="flex-1 min-w-0">
                <textarea @ref="_textareaRef"
                          @bind="BoundValue"
                          @bind:event="oninput"
                          rows="1"
                          placeholder="输入消息..."
                          disabled="@IsLoading"
                          class="w-full bg-slate-50 border border-slate-200 rounded-xl sm:rounded-2xl px-3 sm:px-4 py-2.5 sm:py-3 text-slate-700 text-sm focus:outline-none focus:ring-2 focus:ring-sky-300 focus:border-sky-300 resize-none min-h-[40px] sm:min-h-[48px] max-h-[200px] transition-all shadow-sm"
                          style="field-sizing: content;" />
            </div>
            <button type="button" @onclick="OnSend" disabled="@(CannotSend || IsLoading)"
                    class="h-10 w-10 sm:h-12 sm:w-auto sm:px-5 bg-gradient-to-r from-sky-400 to-emerald-400 text-white font-medium rounded-xl sm:rounded-2xl disabled:opacity-50 disabled:cursor-not-allowed hover:shadow-lg hover:shadow-sky-200/50 hover:scale-105 active:scale-95 transition-all flex items-center justify-center gap-2 flex-shrink-0 shadow-md">
                @if (IsLoading)
                {
                    <svg class="w-5 h-5 animate-spin" fill="none" viewBox="0 0 24 24">
                        <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                        <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                    </svg>
                }
                else
                {
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8"/>
                    </svg>
                }
                <span class="hidden sm:inline">发送</span>
            </button>
        </div>

        @if (!string.IsNullOrEmpty(_errorMessage))
        {
            <p class="text-xs text-red-500 mt-2 ml-1 animate-shake flex items-center gap-1">
                <svg class="w-3.5 h-3.5" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7 4a1 1 0 11-2 0 1 1 0 012 0zm-1-9a1 1 0 00-1 1v4a1 1 0 102 0V6a1 1 0 00-1-1z" clip-rule="evenodd"/>
                </svg>
                @_errorMessage
            </p>
        }
        @* 字符计数提示 *@
        else if (CharCount > 3000)
        {
            <p class="text-xs mt-2 ml-1 @(CharCount > 4000 ? "text-red-500" : "text-amber-500")">
                @(CharCount > 4000 ? "消息过长，建议精简内容" : $"已输入 {CharCount:N0} 字符")
            </p>
        }
    </div>
</footer>

@code {
    [Parameter] public string InputValue { get; set; } = "";
    [Parameter] public EventCallback<string> InputValueChanged { get; set; }
    [Parameter] public bool IsLoading { get; set; }
    [Parameter] public EventCallback OnSendMessage { get; set; }

    /// <summary>
    /// 待发送的图片列表
    /// </summary>
    [Parameter] public List<ImageAttachment> PendingImages { get; set; } = new();
    [Parameter] public EventCallback<List<ImageAttachment>> PendingImagesChanged { get; set; }

    /// <summary>
    /// 图片选择事件（由父组件处理验证和转换）
    /// </summary>
    [Parameter] public EventCallback<InputFileChangeEventArgs> OnFilesSelected { get; set; }

    private ElementReference _textareaRef;
    private DotNetObjectReference<ChatInput>? _dotnetRef;
    private string? _errorMessage;

    private int CharCount => InputValue?.Length ?? 0;
    private bool CannotSend => string.IsNullOrWhiteSpace(InputValue) && PendingImages.Count == 0;

    private string BoundValue
    {
        get => InputValue;
        set => InputValueChanged.InvokeAsync(value);
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            _dotnetRef = DotNetObjectReference.Create(this);
            await JS.InvokeVoidAsync("chatInputHandler.init", _textareaRef, _dotnetRef);
        }
    }



    private async Task HandleFileSelected(InputFileChangeEventArgs e)
    {
        _errorMessage = null;
        await OnFilesSelected.InvokeAsync(e);
    }

    private async Task HandleRemoveImage(ImageAttachment image)
    {
        var images = PendingImages.ToList();
        images.Remove(image);
        await PendingImagesChanged.InvokeAsync(images);
    }

    /// <summary>
    /// 设置错误消息（供父组件调用）
    /// </summary>
    public void SetError(string message)
    {
        _errorMessage = message;
        StateHasChanged();
    }

    /// <summary>
    /// 清除错误消息
    /// </summary>
    public void ClearError()
    {
        _errorMessage = null;
        StateHasChanged();
    }

    /// <summary>
    /// 聚焦输入框（供父组件调用）
    /// </summary>
    public async Task FocusAsync()
    {
        try
        {
            await JS.InvokeVoidAsync("focusTextarea", _textareaRef);
        }
        catch { /* 忽略 JS 调用失败 */ }
    }

    /// <summary>
    /// 供 JS 调用的发送方法
    /// </summary>
    [JSInvokable]
    public async Task SendMessage() => await OnSend();

    /// <summary>
    /// 供 JS 粘贴回调 - 图片粘贴成功
    /// </summary>
    [JSInvokable]
    public async Task OnImagePasted(string base64Data, string mimeType, string fileName)
    {
        var images = PendingImages.ToList();
        images.Add(new ImageAttachment
        {
            Base64Data = base64Data,
            MimeType = mimeType,
            FileName = fileName
        });
        await PendingImagesChanged.InvokeAsync(images);
        _errorMessage = null;
        StateHasChanged();
    }

    /// <summary>
    /// 供 JS 粘贴回调 - 错误处理
    /// </summary>
    [JSInvokable]
    public void OnPasteError(string message)
    {
        _errorMessage = message;
        StateHasChanged();
    }

    private async Task OnSend()
    {
        if (!CannotSend && !IsLoading)
        {
            await OnSendMessage.InvokeAsync();
        }
    }

    public void Dispose()
    {
        _dotnetRef?.Dispose();
    }
}


